# Copiar a .env (sólo desarrollo local; .env está en .gitignore). Sin valores reales aquí.
DATABASE_URL=postgresql+psycopg://greenfield:<clave>@localhost:5432/greenfield
AUTH_ISSUER=
AUTH_AUDIENCE=
AUTH_JWKS_URL=
LOG_LEVEL=INFO
# Sólo pruebas: la base DEBE terminar en _test (se vacía en cada corrida).
TEST_DATABASE_URL=postgresql+psycopg://greenfield:<clave>@localhost:5432/greenfield_test
