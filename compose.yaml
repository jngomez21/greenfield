# PostgreSQL local para correr el servicio a mano (los tests usan testcontainers).
services:
  db:
    image: docker.io/library/postgres:17
    environment:
      POSTGRES_USER: greenfield
      POSTGRES_PASSWORD: greenfield  # sólo desarrollo local
      POSTGRES_DB: greenfield
    ports:
      - "5432:5432"
